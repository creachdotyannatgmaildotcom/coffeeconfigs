#server:
#  port: 8070
#
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    fetchRegistry: false  # never gonna call other microservices so don't have to fetch the registry eg: list
#    registerWithEureka: false # not registering himself
#    
logging:
  level:
    com.netflix.discovery: debug
    com.netflix.loadbalancer: debug
